from setuptools import setup, find_packages

setup(
    name="hsc_master",
    version="0.0.1",
    description="Hsc Master",
    author="duxdigitech",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)
